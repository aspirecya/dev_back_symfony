{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row custyle">
            <table class="table table-striped custab">
                <thead>
                <a href="{{ path('admin_post_add') }}" class="btn btn-primary btn-xs pull-right mb-4"><b>+</b> Add new post</a>
                <tr>
                    <th>ID</th>
                    <th>Titre</th>
                    <th>Aperçu</th>
                    <th>Date</th>
                    <th class="text-center">Action</th>
                </tr>
                </thead>
                {% for post in posts %}
                    <tr>
                        <td>{{ post.id }}</td>
                        <td>{{ post.title }}</td>
                        <td>{{ post.content | slice(0, 40) }}...</td>
                        <td>{{ post.datePosted | date('d/m/Y') }}</td>
                        <td class="text-center"><a class='btn btn-info btn-xs' href="{{ path('admin_post_edit', { id: post.id }) }}"><i class="far fa-edit"></i> Edit</a> <a href="{{ path('admin_post_delete', { id: post.id }) }}" class="btn btn-danger btn-xs"><i class="far fa-trash-alt"></i> Del</a></td>
                    </tr>
                {% endfor %}
            </table>
        </div>
    </div>
{% endblock %}