{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row custyle">
            <table class="table table-striped custab">
                <thead>
                <a href="#" class="btn btn-primary btn-xs pull-right"><b>+</b> Add new categories</a>
                <tr>
                    <th>ID</th>
                    <th>Titre</th>
                    <th>Aperçu</th>
                    <th>Date</th>
                    <th class="text-center">Action</th>
                </tr>
                </thead>
                {% for post in posts %}
                    <tr>
                        <td>{{ post.id }}</td>
                        <td>{{ post.title }}</td>
                        <td>{{ post.content | slice(0, 40) }}...</td>
                        <td>{{ post.datePosted | date('d/m/Y') }}</td>
                        <td class="text-center"><a class='btn btn-info btn-xs' href="#"><i class="far fa-edit"></i> Edit</a> <a href="#" class="btn btn-danger btn-xs"><i class="far fa-trash-alt"></i> Del</a></td>
                    </tr>
                {% endfor %}
            </table>
        </div>
    </div>
{% endblock %}